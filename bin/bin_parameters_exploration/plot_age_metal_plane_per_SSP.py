from lib_spm import *

#out_dir = os.path.join('/data42s/comparat/firefly/v1_1_0/figures', 'mass-redshift-presentation')
out_dir = os.path.join(os.environ['HOME'], 'software/linux/firefly_explore', 'data/images', 'az')

def plot_az_boss(imf=imfs[0], ii=0):
	stellar_mass = imf+'stellar_mass'
	ssp_MW = imf+'weightMass_ssp_'+str(ii)
	age = imf+'age_ssp_'+str(ii)
	metal = imf+'metal_ssp_'+str(ii)
	# boss
	redshift_reliable_boss =  (boss['CLASS_NOQSO'] == "GALAXY") & ( boss['Z_ERR_NOQSO'] > 0.0) & (boss['ZWARNING_NOQSO'] == 0) & (boss['Z_NOQSO']>0.001) & (boss['Z_NOQSO'] > boss['Z_ERR_NOQSO'] ) # (boss['SN_MEDIAN_ALL'] > 0.1 ) & 
	error_reliable_boss = (boss[stellar_mass+'_up_1sig'] > boss[stellar_mass+'_low_1sig'] ) & (boss[stellar_mass+'_up_1sig'] > 0. ) & ( boss[stellar_mass+'_low_1sig'] > 0. ) & (boss[stellar_mass+'_up_1sig'] < 1e14 ) & ( boss[stellar_mass+'_low_1sig'] < 1e14 ) 
	mass_reliable_boss_04 = (boss[stellar_mass] > 1e6 ) & ( boss[stellar_mass] < 1e14 ) & ((n.log10(boss[stellar_mass+'_up_1sig']) - n.log10(boss[stellar_mass+'_low_1sig']))/2. < 0.4 )
	ok_boss_04 = (error_reliable_boss) & (mass_reliable_boss_04) & (redshift_reliable_boss)
	A_04_ref = n.log10(boss[age][ok_boss_04]           )
	Z_04_ref = n.log10(boss[metal][ok_boss_04]         )
	ssp_MW_ref = boss[ssp_MW][ok_boss_04]         
	a_bins = n.arange(6.5, 10.5, 0.1)
	z_bins = n.arange(-3,3,0.1)
	XX, YY = n.meshgrid((z_bins[1:]+z_bins[:-1])/2., 0.5*(a_bins[1:]+a_bins[:-1]))
	HH = n.histogram2d(Z_04_ref, A_04_ref, bins=[z_bins, a_bins], weights=ssp_MW_ref)[0].T
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	p.ylabel(r'$\log_{10}(Age/yr)$')
	p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.grid()
	p.text(-2.1, 7.5,'eBOSS '+imf.split('_')[1]+' '+str(ii) )
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "age_metallicity_"+ssp_MW+"_eboss_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	#p.ylabel(r'$\log_{10}(Age/yr)$')
	p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'eBOSS '+imf.split('_')[1]+' '+str(ii) )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_Y_age_metallicity_"+ssp_MW+"_eboss_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	p.ylabel(r'$\log_{10}(Age/yr)$')
	#p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'eBOSS '+imf.split('_')[1]+' '+str(ii) )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_X_age_metallicity_"+ssp_MW+"_eboss_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	#p.ylabel(r'$\log_{10}(Age/yr)$')
	#p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.grid()
	p.text(-2.1, 7.5,'eBOSS '+imf.split('_')[1]+' '+str(ii)  )
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_XY_age_metallicity_"+ssp_MW+"_eboss_04.png" ))
	p.clf()


def plot_az_sdss(imf=imfs[0], ii=0):
	stellar_mass = imf+'stellar_mass'
	ssp_MW = imf+'weightMass_ssp_'+str(ii)
	age = imf+'age_ssp_'+str(ii)
	metal = imf+'metal_ssp_'+str(ii)
	# sdss
	redshift_reliable_sdss =  (sdss['CLASS'] == "GALAXY")       & ( sdss['Z_ERR'] > 0.0)       & (sdss['ZWARNING'] == 0)       & (sdss['Z'] > 0.001) & (sdss['Z'] > sdss['Z_ERR'] ) 
	error_reliable_sdss = (sdss[stellar_mass+'_up_1sig'] > sdss[stellar_mass+'_low_1sig'] ) & (sdss[stellar_mass+'_up_1sig'] > 0. ) & ( sdss[stellar_mass+'_low_1sig'] > 0. ) & (sdss[stellar_mass+'_up_1sig'] < 1e14 ) & ( sdss[stellar_mass+'_low_1sig'] < 1e14 ) 
	mass_reliable_sdss_04 = (sdss[stellar_mass] > 1e6 ) & ( sdss[stellar_mass] < 1e14 ) & ((n.log10(sdss[stellar_mass+'_up_1sig']) - n.log10(sdss[stellar_mass+'_low_1sig']))/2. < 0.4 )
	ok_sdss_04 = (error_reliable_sdss) & (mass_reliable_sdss_04) & (redshift_reliable_sdss)
	#
	A_04_ref = n.log10(sdss[age][ok_sdss_04]           )
	Z_04_ref = n.log10(sdss[metal][ok_sdss_04]         )
	ssp_MW_ref = sdss[ssp_MW][ok_sdss_04]         
	a_bins = n.arange(6.5, 10.5, 0.1)
	z_bins = n.arange(-3,3,0.1)
	XX, YY = n.meshgrid((z_bins[1:]+z_bins[:-1])/2., 0.5*(a_bins[1:]+a_bins[:-1]))
	HH = n.histogram2d(Z_04_ref, A_04_ref, bins=[z_bins, a_bins], weights=ssp_MW_ref)[0].T
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	p.ylabel(r'$\log_{10}(Age/yr)$')
	p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.grid()
	p.text(-2.1, 7.5,'SDSS '+imf.split('_')[1]+' '+str(ii) )
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "age_metallicity_"+ssp_MW+"_sdss_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	#p.ylabel(r'$\log_{10}(Age/yr)$')
	p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'SDSS '+imf.split('_')[1]+' '+str(ii) )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_Y_age_metallicity_"+ssp_MW+"_sdss_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	p.ylabel(r'$\log_{10}(Age/yr)$')
	#p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'SDSS '+imf.split('_')[1]+' '+str(ii) )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_X_age_metallicity_"+ssp_MW+"_sdss_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s', vmin=1, vmax=4 )
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	#p.ylabel(r'$\log_{10}(Age/yr)$')
	#p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N \times w_M)$')
	p.legend(loc=0, frameon = False)
	p.grid()
	p.text(-2.1, 7.5,'SDSS '+imf.split('_')[1]+' '+str(ii)  )
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_XY_age_metallicity_"+ssp_MW+"_sdss_04.png" ))
	p.clf()

def plot_az_deep2(imf=imfs[0],ii=0):
	stellar_mass = imf+'stellar_mass'
	ssp_MW = imf+'weightMass_ssp_'+str(ii)
	age = imf+'log_age_ssp_'+str(ii)
	metal = imf+'metal_ssp_'+str(ii)
	z_flg = 'ZQUALITY'
	z_name = 'ZBEST'
	deep2_zOk = (deep2[z_name] > 0.6) & (deep2[z_flg]>=2.) & (deep2[z_name] < 1.2) & (deep2['SSR']>0) & (deep2['TSR']>0) & (deep2['SSR']<=1.0001) & (deep2['TSR']<=1.0001)
	deep2_sel_04 = (deep2_zOk) & (deep2[stellar_mass] < 10**14. ) & (deep2[stellar_mass] > 0. )  & (deep2[stellar_mass] >= deep2[stellar_mass+'_low_1sig'] ) & (deep2[stellar_mass] <= deep2[stellar_mass+'_up_1sig'] ) & ( - n.log10(deep2[stellar_mass+'_low_1sig'])  + n.log10(deep2[stellar_mass+'_up_1sig']) < 0.8 )
	A_04_ref = n.log10(deep2[age][deep2_sel_04]           )
	Z_04_ref = n.log10(deep2[metal][deep2_sel_04]         )
	ssp_MW_ref = deep2[ssp_MW][deep2_sel_04]         
	a_bins = n.arange(6.5, 10.5, 0.1)
	z_bins = n.arange(-3,3,0.1)
	XX, YY = n.meshgrid((z_bins[1:]+z_bins[:-1])/2., 0.5*(a_bins[1:]+a_bins[:-1]))
	HH = n.histogram2d(Z_04_ref, A_04_ref, bins=[z_bins, a_bins], weights=ssp_MW_ref)[0].T
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s' , vmin=1, vmax=2.5)
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	p.ylabel(r'$\log_{10}(Age/yr)$')
	p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'DEEP2 '+imf.split('_')[1]+' '+str(ii)  )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "age_metallicity_"+ssp_MW+"_deep2_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s' , vmin=1, vmax=2.5)
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	p.ylabel(r'$\log_{10}(Age/yr)$')
	#p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'DEEP2 '+imf.split('_')[1]+' '+str(ii)  )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_X_age_metallicity_"+ssp_MW+"_deep2_04.png" ))
	p.clf()
	p.figure(0, (5.5, 4.5))
	#
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s' , vmin=1, vmax=2.5)
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	#p.ylabel(r'$\log_{10}(Age/yr)$')
	p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'DEEP2 '+imf.split('_')[1]+' '+str(ii)  )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_Y_age_metallicity_"+ssp_MW+"_deep2_04.png" ))
	p.clf()
	#
	p.figure(0, (5.5, 4.5))
	p.axes([0.2,0.2,0.7,0.7])
	p.scatter(XX[HH>1], YY[HH>1], c=n.log10(HH[HH>1]), s=40, edgecolors='none', marker='s' , vmin=1, vmax=2.5)
	p.xlim((-2.2,0.5))
	p.ylim((7.3,10.5))
	#p.ylabel(r'$\log_{10}(Age/yr)$')
	#p.xlabel(r'$\log_{10}(Z/Z_\odot)$')
	p.colorbar(shrink=0.7, label=r'$\log_{10}(N)$')
	p.legend(loc=0, frameon = False)
	p.text(-2.1, 7.5,'DEEP2 '+imf.split('_')[1]+' '+str(ii)  )
	p.grid()
	p.tight_layout()
	p.savefig(os.path.join(out_dir, "no_XY_age_metallicity_"+ssp_MW+"_deep2_04.png" ))
	p.clf()

for ssp_id in n.arange(8):
	plot_az_deep2(imf = imfs[0], ii=ssp_id)
	plot_az_deep2(imf = imfs[1], ii=ssp_id)
	plot_az_deep2(imf = imfs[2], ii=ssp_id)
	plot_az_deep2(imf = imfs[3], ii=ssp_id)
	plot_az_deep2(imf = imfs[4], ii=ssp_id)
	plot_az_deep2(imf = imfs[5], ii=ssp_id)
	plot_az_deep2(imf = imfs[6], ii=ssp_id)
	plot_az_deep2(imf = imfs[7], ii=ssp_id)
	plot_az_deep2(imf = imfs[8], ii=ssp_id)

for ssp_id in n.arange(9):
	plot_az_sdss(imf = imfs[0], ii=ssp_id)
	plot_az_sdss(imf = imfs[1], ii=ssp_id)
	plot_az_sdss(imf = imfs[2], ii=ssp_id)
	plot_az_sdss(imf = imfs[3], ii=ssp_id)
	plot_az_sdss(imf = imfs[4], ii=ssp_id)
	plot_az_sdss(imf = imfs[5], ii=ssp_id)
	plot_az_sdss(imf = imfs[6], ii=ssp_id)
	plot_az_sdss(imf = imfs[7], ii=ssp_id)
	plot_az_sdss(imf = imfs[8], ii=ssp_id)


for ssp_id in n.arange(9):
	plot_az_boss(imf = imfs[0], ii=ssp_id)
	plot_az_boss(imf = imfs[1], ii=ssp_id)
	plot_az_boss(imf = imfs[2], ii=ssp_id)
	plot_az_boss(imf = imfs[3], ii=ssp_id)
	plot_az_boss(imf = imfs[4], ii=ssp_id)
	plot_az_boss(imf = imfs[5], ii=ssp_id)
	plot_az_boss(imf = imfs[6], ii=ssp_id)
	plot_az_boss(imf = imfs[7], ii=ssp_id)
	plot_az_boss(imf = imfs[8], ii=ssp_id)
